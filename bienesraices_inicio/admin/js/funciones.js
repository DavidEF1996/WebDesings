$(document).ready(function(){
    //modal form add seccion
    $('.add_section').click(function (e) { 
        e.preventDefault();
        //var nombre = $(this).attr('section');
        alert("nombre");
    });
});